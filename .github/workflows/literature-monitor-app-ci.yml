name: Literature Monitor App CI

on:
  push:
    branches: [literature-monitor-app]
    paths:
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "tsconfig.base.json"
      - "packages/**"
      - "apps/**"
      - ".github/workflows/literature-monitor-app-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm install
      - run: npm test -- packages/literature-core/tests
      - run: npm --workspace apps/worker test
      - run: npm --workspace apps/extension test
      - run: npm run typecheck
      - run: npm --workspace apps/extension run build
      - name: Package Chrome/Edge extension
        run: |
          cd apps/extension/dist
          zip -qr ../literature-monitor-b-lite.zip .
      - uses: actions/upload-artifact@v4
        with:
          name: literature-monitor-b-lite
          path: apps/extension/literature-monitor-b-lite.zip
          if-no-files-found: error
      - name: Wrangler dry-run bundle
        run: npm --workspace apps/worker exec -- wrangler deploy --dry-run --outdir /tmp/literature-worker-dist
